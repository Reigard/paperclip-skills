Weekly Support maintenance health check for [CLIENT] Craft CMS production.
Structured maintenance payload:
Target (production, anonymous/public only): https://[CLIENT-DOMAIN]/
Staging https://[CLIENT-STAGING].designingit.co/ is OUT OF SCOPE - do not audit it and do not flag its noindex/robots hygiene in this report.
Orchestrator - follow the support-maintenance-orchestration skill exactly:
Create assigned CLOSED-LIST children, then STOP:
craft-basic      -> Craft CMS Health Audit Agent
frontend-basic   -> Frontend / Browser QA Agent
performance-basic -> Frontend / Browser QA Agent
On a later wake, verify each specialist native work product, create a report-triage child -> Report / Triage Agent, then STOP.
On a later wake after triage passes, run DIT Monitoring ingest following the paperclip-dit-monitoring skill, then STOP.
On a later wake, attach the parent final-report.html native work product and mark done.
One wake stage at a time.
DIT Monitoring ingest (paperclip-dit-monitoring skill): in-agent only — read task folder, build JSON, HTTP POST; no host CLI helper. Gates (all required): report-triage pass or pass_with_notes; task folder has findings.json, final-report.html, and findings/*.json for each selected check; DIT_MONITORING_ID resolved from the current client project ([CLIENT]); DIT_MONITORING_INGEST_URL and DIT_MONITORING_INGEST_TOKEN resolved from the Support System run environment (not placeholders). Config: DIT_MONITORING_ID is stored on the current client project ([CLIENT]). DIT_MONITORING_INGEST_URL is a Support System environment variable. DIT_MONITORING_INGEST_TOKEN is a Support System environment variable backed by the company secret dit_monitoring_ingest_token. The agent must use DIT_MONITORING_INGEST_TOKEN from the run environment only and must never attempt to resolve the company secret directly or read the ingest token from the issue payload or agent host environment. Routine payload may carry dit_monitoring_id and dit_monitoring_ingest_url only — never put the ingest token in the payload (the payload is copied into the issue text). One POST per run_id. Not before triage. Failures → _sync.warnings and a short comment; do not block the run.
Craft CMS Health Audit Agent (craft-cms-health-audit skill, Playwright + Craft MCP): follow the craft-cms-health-audit skill exactly. Environment is production. MCP endpoint and token are resolved from the current client project secrets (CRAFT_MCP_URL, CRAFT_MCP_TOKEN) — if not present, skip MCP introspection checks (steps 2–8) and note "MCP Introspection skipped (not configured)" in the report. CP login credentials are resolved from client project secrets (CRAFT_CP_USER, CRAFT_CP_PASSWORD). Forms config is resolved from client project plain env (CRAFT_FORMS_CONFIG). Run all 7 Playwright smoke tests (Availability, Templates, Console, Links, SEO, Forms E2E, Control Panel). Read-only — no php craft update, no project-config/apply, no queue/run, no database writes.
Front-end (Chrome DevTools MCP, Chrome 150): capture JS console errors, failed network requests, one screenshot, and a load-speed / Core Web Vitals proxy trace (label lab vs field). Add a mobile-viewport emulate pass + throttled Lighthouse mobile when feasible. No flow-smoke, form submits, load tests, or destructive checks.
Red flags = agent judgment of anomalies that are out of normal for this site: devMode enabled on production, project-config out of sync, Craft core license invalid or expired, Control Panel login failure, contact form submission failure, a failing Core Web Vital, a large load-time regression, robots.txt / meta-robots blocking indexing of pages that should be indexable, or JavaScript console errors that break functionality. Do NOT raise site-down / reachability as a red flag here - that is handled by dedicated 1-minute uptime monitoring, not this weekly job.
