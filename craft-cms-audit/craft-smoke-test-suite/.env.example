# Copy to .env for local runs, or set as runtime secrets.
# .env is gitignored — never commit real values.

SMOKE_BASE_URL=https://your-site.com
SMOKE_SLACK_WEBHOOK=
SMOKE_CP_USERNAME=
SMOKE_CP_PASSWORD=
